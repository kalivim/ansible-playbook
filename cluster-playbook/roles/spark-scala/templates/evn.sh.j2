#!/bin/bash

#统计
# /////////////////////////////
export host={{ gm_mysql }}
export db={{ gm_mysql_db }}
export user={{ gm_mysql_user }}
export pwd={{ gm_mysql_passwd }}

# hdfs地址
export dfs=hdfs://{{ hdfs_ip }}:{{ hdfs_port }}

source /etc/profile

